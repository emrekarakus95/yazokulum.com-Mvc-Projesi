@model yazokulum.com.Models.IndexViewModel
@{
    ViewBag.Title = "Yönet";
}

<h2>@ViewBag.Title.</h2>

<p class="text-success">@ViewBag.StatusMessage</p>
<div>
    <h4>Hesap ayarlarınızı değiştirin</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Parola:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Parolanızı değiştirin", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Oluştur", "SetPassword")
            }
            ]
        </dd>
        <dt>Dışarıdan Oturum Açma:</dt>
        <dd>
            @Model.Logins.Count [
            @Html.ActionLink("Yönet", "ManageLogins") ]
        </dd>
        @*
            Telefon Numaraları, iki öğeli kimlik doğrulama sisteminde ikinci doğrulama öğesi olarak kullanılabilir.
             
              SMS ile iki öğeli kimlik doğrulamayı destekleyen bu ASP.NET uygulamasının kurulumu hakkında ayrıntılı bilgi edinmek için <a href="http://go.microsoft.com/fwlink/?LinkId=403804">bu makaleye</a>
                göz atın.
             
             İki öğeli kimlik doğrulama kurulumundan sonra aşağıdaki blok açıklamasını kaldırın
        *@
        @*  
            <dt>Telefon Numarası:</dt>
            <dd>
                @(Model.PhoneNumber ?? "None")
                @if (Model.PhoneNumber != null)
                {
                    <br />
                    <text>[&nbsp;&nbsp;@Html.ActionLink("Change", "AddPhoneNumber")&nbsp;&nbsp;]</text>
                    using (Html.BeginForm("RemovePhoneNumber", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        <text>[<input type="submit" value="Remove" class="btn-link" />]</text>
                    }
                }
                else
                {
                    <text>[&nbsp;&nbsp;@Html.ActionLink("Add", "AddPhoneNumber")
                }
            </dd>
        *@
        <dt>İki Öğeli Kimlik Doğrulama:</dt>
        <dd>
            <p>
                Yapılandırılmış bir iki öğeli kimlik doğrulama yok. İki öğeli kimlik doğrulamayı destekleyen bu ASP.NET uygulamasının kurulumu hakkında ayrıntılı bilgi edinmek için <a href="http://go.microsoft.com/fwlink/?LinkId=403804">bu makaleye</a>
                göz atın.
            </p>
            @*@if (Model.TwoFactor)
                {
                    using (Html.BeginForm("DisableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        <text>Etkinleştirildi
                        <input type="submit" value="Devre dışı bırak" class="btn btn-link" />
                        </text>
                    }
                }
                else
                {
                    using (Html.BeginForm("EnableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        <text>Devre dışı bırakıldı
                        <input type="submit" value="Etkinleştir" class="btn btn-link" />
                        </text>
                    }
                }*@
        </dd>
    </dl>
</div>
